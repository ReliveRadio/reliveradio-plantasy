namespace :application do
  desc "SCP transfer application configuration to the shared folder"
  task :setup do
  	on roles(:app) do
      execute "mkdir -p #{shared_path}/config"
      upload! my_template("application.yml.erb"), "#{shared_path}/config/application.yml"
    end
  end
end