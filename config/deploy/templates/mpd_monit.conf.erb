check process <%= "mpd_#{fetch(:application)}_#{fetch(:stage)}" %> with pidfile /var/run/mpd/pid_<%= fetch(:full_app_name) %>
  start program = "/etc/init.d/mpd_<%= fetch(:full_app_name) %> start"
  stop program  = "/etc/init.d/mpd_<%= fetch(:full_app_name) %> stop"
  group mpd