
<div class="row">
	<div class="medium-12 columns">
		
		<h1>Channels</h1>
		<ul>
			<% @channel_playlists.each do |channel_playlist| %>
				<li><%= link_to channel_playlist.name, schedule_show_path(channel_playlist) %></li>
			<% end %>
		</ul>
	</div>
</div>

