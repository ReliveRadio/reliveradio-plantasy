<%= form_for(@jingle) do |f| %>
  <% if @jingle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@jingle.errors.count, "error") %> prohibited this jingle from being saved:</h2>

      <ul>
      <% @jingle.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label 'Audio' %><br>
    <%= f.file_field :audio %>
  </div>
  <div class="actions">
    <%= f.submit 'Save', class: 'button' %>
  </div>
<% end %>
